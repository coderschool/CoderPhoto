<table class="table">
  <tr>
    <td>ID</td>
    <th>Comments</th>
    <th></th>
  </tr>
  <% @photos.each do |photo| %>
    <tr>
      <td><%= photo.id %></td>
      <td><%= photo.comments.size %></td>
      <td>
        <%= link_to photo_comments_path(photo, format: :json), photo_comments_path(photo, format: :json) %>
      </td>
    </tr>
  <% end %>
</table>
